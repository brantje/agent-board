name: Consolidate Users and Groups

on:
  push:
    branches:
      - "phase/79-authz-hardening"

permissions:
  contents: write

jobs:
  consolidate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: "phase/79-authz-hardening"
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version-file: apps/server/go.mod
          cache-dependency-path: apps/server/go.sum

      - name: Apply consolidation
        run: python3 .github/consolidate-users-groups.py

      - name: Format and verify focused server packages
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t go_files < <(git diff --name-only --diff-filter=ACMR -- '*.go')
          if ((${#go_files[@]})); then
            gofmt -w "${go_files[@]}"
          fi
          if git ls-files apps packages | grep -Ei '(^|[/_.-])phase[_-]?[1-5]([/_.-]|$)|auth_phase'; then
            echo 'phase-derived source filenames remain' >&2
            exit 1
          fi
          if git grep -nEi '\b(phase[_ -]?[1-5]|phase[1-5]|AuthPhase[1-5])\b' -- apps packages; then
            echo 'phase-derived source terminology remains' >&2
            exit 1
          fi
          cd apps/server
          go test ./internal/app ./internal/httpapi ./internal/store/...
          go vet ./internal/app ./internal/httpapi ./internal/store/...
          go build ./...

      - name: Commit consolidation
        shell: bash
        run: |
          set -euo pipefail
          git status --short
          git config user.name 'agent-board maintenance'
          git config user.email 'agent-board-maintenance@users.noreply.github.com'
          git add -A
          git commit -m 'refactor: consolidate Users and Groups terminology'
          git push origin HEAD:phase/79-authz-hardening
