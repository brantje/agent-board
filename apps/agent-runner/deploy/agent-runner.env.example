# External runners persist their Agent Board URL, runner ID, and credential in
# /var/lib/agent-runner/runner.json after `agent-runner register`.

# Optional. Defaults to /var/lib/agent-runner/workspaces.
AGENT_RUNNER_WORKSPACE_ROOT=/var/lib/agent-runner/workspaces
